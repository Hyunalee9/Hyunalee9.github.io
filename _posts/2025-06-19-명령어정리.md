---
title: "[41일차] 리눅스 명령어 정리"
excerpt: "아이티윌 0619 "
categories:
      - ORACLE11g
tags:
      - ORACLE11g
      - TIL
last-modified-at: 2025-06-19T21:02
---


# Linux 명령어 정리

```bash
mv(move) -- 파일을 이동하는 명령어
mv ./test1 ./test5 -- 디렉토리를 다른 디렉토리에 이동
mv ex1.txt ex.txt -- 파일 이름 수정

▶️ mv 옵션 
- i :이동될 파일이 존재하면 덮어쓰기 여부를 물음
- b :이동될 파일이 존재하면 백업 파일을 생성한다.
- f :이동될 파일이 존재하면 덮어쓰기 한다.
- v :이동 진행 상태를 출력한다.

vi(visual editor) -- 문서 편집기

1. 명령 모드 (command mode)
vi 명령어로 편집기를 시작할 시 단축키를 사용하여 커서 이동, 수정, 삭제, 복사, 붙이기 등 다양한
명령을 수행할 수 있다.

입력 모드에 진입한 후 다시 명령모드로 돌아오려면 [esc]키를 누르면 명령 모드로 다시 되돌아올 수 있다.

2. 입력 모드 (insert mode)
명령 모드 상태에서 i, a, o, l, A, O의 단축키를 입력하여 입력 모드로 전환할 수 있다.
입력 모드에서는 직접 글이나 코드를 작성할 수 있는 상태이다.

3. 마지막 행 모드        
마지막 행 모드는 명령 모드 상태에서 콜론(:)을 입력하면 화면 맨 밑에서 명령을 수행하는 모드이다.
저장, 종료, 치환, 탐색등의 기능을 수행할 수 있다.

▶️ 편집기를 시작하는 명령어
vi 파일명 : 읽기, 쓰기 가능한 상태로 파일이 열린다.
view 파일명 : 읽기만 가능한 상태로 파일이 열린다.

▶️ 커서 이동
- h : 왼쪽 이동
- j : 아래 이동
- k : 위로 이동
- l : 오른쪽 이동
- H : 화면 맨 위로 이동
- M : 화면 중간으로 이동
- L : 화면 맨 아래로 이동
- G : 문서 맨 아래로 이동
- 1G : 숫자 라인으로 이동
- gg : 문서 제일 첫 라인으로 이동
- ^  : 그 줄의 맨앞으로 이동 
- $  : 그 줄의 맨 뒤로 이동
- :n : n번째 숫자 라인으로 이동
- +  : 커서를 다음 행의 처음으로 이동
- -  : 커서를 앞 행의 처음으로 이동
- w  : 커서를 다음 단어 첫 글자로 이동
- e  : 커서를 다음 단어 마지막 글자로 이동
- b  : 커서를 앞 단어의 첫 글자로 이동 

- Ctrl + b : page up
- Ctrl + f : page down
- Ctrl + u : page up(1/2)
- Ctrl + d : page down(1/2)

- :set nu  : 라인번호 출력

▶️ 행번호
- :set nu   : 라인 번호 출력
- :set nonu : 라인 번호 출력 해지

▶️ 문자열 검색
/문자       : 문자 검색
						알파벳 n을 누르면 검색된 다음 문자로 이동
						shift + n 검색된 문자 앞으로 이동

▶️ 검색 문자열이 블록 처리를 해지
- :noh     	

▶️ 입력 명령어 
- i       : 커서의 왼쪽부터 입력
- a       : 커서의 오른쪽부터 입력
- l       : 커서의 줄 맨 앞에서부터 입력
- A       : 커서의 줄 맨 뒤에서부터 입력
- o       : 커서의 다음줄부터 입력
- O       : 커서의 윗줄에 입력
- s       : 커서 위치 한 글자를 지우고 입력
- S       : 커서 위치 한 줄을 지우고 입력
- cw      : 커서 위치의 단어를 지우고 입력

▶️ 삭제 명령어
- x       : 한 글자씩 삭제
- dd      : 한 행 삭제
- dw      : 커서에 있는 단어 삭제
- :5 d    : 5번째 행 삭제
- :5,6 d  : 5번째 6번째 행 삭제
- D       : 커서 오른쪽 행 삭제

▶️ 복사, 붙여넣기
- yy      : 커서 행 복사
- nyy     : 커서부터 숫자행만큼 복사
- yG      : 현재부터 파일 끝까지 복사
- p       : 복사한 내용을 현재 행 이후 붙여넣기
- P       : 복사한 내용을 현재 행 이전에 붙여넣기
- :1,2 co 3 : 1,2 행을 복사해서 3행 다음으로 붙여넣기
- :1,2 m  3 : 1,2 행을 3행 다음으로 이동

▶️ 취소 명령어
- u       : 방금 수행한 작업 취소
- Ctrl + r : redo

▶️ 문자열 검색해서 수정
- :s/기존문자/변경할문자 : 커서가 있는 위치에서 첫번째로 나오는 기존문자를 변경할 문자로 변경
- :s/기존문자/변경할문자/g : 커서가 있는 행의 기존문자를 변경할 문자로 다 변경한다.
- :%s/기존문자/변경할문자/g : 문서 전체에서 기존 문자를 변경할 문자로 다 변경한다.

▶️ 파일 저장 및 종료
- :q      : 저장하고 종료 
- :q!     : 저장하지 않고 vi 종료
- :wq     : 저장하고 vi 종료
- :w      : 저장(vi 종료하지 않고 계속 편집)
- :w  파일명 : 파일명으로 저장한 후 계속 편집
- :wq 파일명 : 파일명으로 저장하고 vi 종료 

▶️ 사용자 계정 관리
- 리눅스는 다중 사용자 시스템 (multi user system)
					1 대의 리눅스에 사용자 여려명이 접속해서 사용할 수 있는 시스템이다.
- 사용자는 하나 이상의 그룹에 소속되어 있어야 한다.

▶️ 사용자 계정 정보
cat /etc/passwd			

▶️
itwill:x:1000:1000:itwill:/home/itwill:/bin/bash
사용자이름:암호:사용자ID:사용자가 소속된 그룹ID:전체이름:홈디렉토리:기본셀(SHELL쉘)

▶️ 그룹 정보
[root@oracle ~]# cat /etc/group

itwill:x:1000:itwill
그룹이름:비밀번호:그룹ID:그룹속한유저

▶️ 유저 생성하는 명령어
useradd adduser

▶️ 사용자의 비밀번호를 저장하거나 변경하는 명령어
passwd
		
▶️ 패스워드 관리하는 파일
[root@hyuna ~]# cat /etc/shadow | grep user1
user1:!!:20257:0:99999:7:::

사용자 이름:암호화된패스워드:마지막변경:패스워드 최소 사용기간:패스워드 최대 사용기간:경고:비활성화:만료일
--1970년 1월 1일 이후 20258일이 지났다.

▶️ 계정 삭제하는 명령어
userdel -rf user2

옵션
-r      : 유저가 생성한 파일, 디렉토리를 삭제
-f      : 유저가 리눅스에 접속을 했더라도 kill 시킨 후 삭제 

▶️ 파일 접근 권한 관리
-rw-r--r--. 1 root root 2310 Jun 18 23:39 passwd

▶️ 권한 종류 3가지
1. 읽기(read) 권한
2. 쓰기(write) 권한
3. 실행(execute) 권한
		
```

![image.png](/assets/20250619/1.png)

```bash
ls -l passwd
-rw-r--r--. 1 root root 2310 Jun 18 23:39 passwd
파일 유형 파일허가 링크수 파일소유자 파일소유그룹 파일크기 마지막변경날짜시간 파일이름

▶️ 파일 유형
파일이 어떤 종류인지 나타낸다.
- : 파일
d : 디렉토리
l : 심볼링크(윈도우 바로가기)
b : 블록 디바이스 장치(block device) : 하드디스크, 플로피디스크, CD/DVD 저장
c : 문자 디바이스장치(character device) : 마우스, 키보드, 프린터등의 입출력장치

ls -l  /dev |more

▶️ 파일 허가
rw- r-- r--
--  --- ---
1)  2)  3)

1) 파일의 소유자 권한
2) 그룹의 권한
3) 기타 사용자 권한

▶️ chmod
권한을 수정하는 명령어

옵션
u      : 유저
g      : 그룹
o      : 기타 유저
a      : 전체

r      : 읽기(read)
w      : 쓰기(write)
x      : 실행(execute)

+      : 권한 부여
-      : 권한 해지

[root@oracle ~\# ls -l passwd

▶️ chmod u+x passwd

-- copy
-- cat

▶️ 권한을 설정하는 숫자
8진수 각 숫자는 권한 패턴
0 ~ 7
x -> 2**0 = 1
w -> 2**1 = 2
r -> 2**2 = 4

▶️ 8진수 rwx
   ----- ----
   0     ---
   1     --x
   2     -w-
   3     -wx
   4     r--
   5     r-x
   6     rw-
   7     rwx
   
   
r w x   r w x   r w x
4 2 1   4 2 1   4 2 1 
  7       7       7

chmod 000 passwd -- 권한 모두 회수  
chmod 777 passwd -- 
chmod 744 passwd -- 
chmod 644 passwd -- 

chmod 555 passwd
chmod 755 passwd -- 소유자만큼은 7이라는 권한이 있어야 한다.

▶️ 기본 접근 권한
리눅스에서는 파일이나 디렉토리를 생성할 때 기본 접근 권한이 자동으로 설정된다.

▶️ cat .bashrc (환경파라미터)
   
```

![image.png](/assets/20250619/2.png)

- 리눅스에서는 기본적으로 파일을 생성하면 실행권한(x)를 갖고 생성하지 못하도록 되어 있다.
- 일반 파일의 최대 권한

```bash
rw- rw- rw-
110 110 110  <-  2진수 변환(666)
000 000 010  <-  마스크값(002)
666 - 002 = 664

디렉토리의 최대 권한 
rwx rwx rwx
111 111 111  <- 2진수 변환(777)
000 000 010  <- 마스크값(002)

777 - 002 = 775             
```